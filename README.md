# GameVoice — каталог русификаторов и озвучек

Статичный сайт (HTML/CSS/JS, без сборки) — каталог русских локализаций игр
в стиле самиздат/broadsheet: тёплая бумага, типографская краска, газетные заголовки.

**Live:** [stintik-123.github.io/GameVoice](https://stintik-123.github.io/GameVoice/)  
**Репозиторий:** [github.com/Stintik-123/GameVoice](https://github.com/Stintik-123/GameVoice)

## Структура

```
/
├── index.html              # Разметка, preload CSS, og:image, модалки
├── manifest.webmanifest    # PWA
├── README.md
├── .gitignore
├── css/
│   ├── style.css           # Тема самиздат (часть A)
│   └── style-b.css         # Карточки, фильтры, лента, хоткеи (часть B)
└── js/
    ├── data.js             # База: игры, варианты, реальные ссылки
    ├── app.js              # Логика (поиск, фильтры, избранное, рейтинг, лента)
    └── hero-video.js       # Фоновый muted-трейлер YouTube (десктоп)
```

## Возможности

- Поиск (`/`) по названию, студии, автору, жанру, тегам + транслит (киберпанк ↔ cyberpunk)
- Фильтры: тип / статус / платформа + быстрые чипы
- Сортировка: рейтинг · **дата обновления** · год · число вариантов · алфавит
- Виды каталога: **сетка** · **список** · **лента** (carousel)
  - в ленте — горизонтальная прокрутка колёсиком (без блокировки страницы, когда крутить некуда)
- Избранное и история (localStorage); избранное поднимается вверх **только без активных фильтров**
- Плашка **«Новинка»** на карточках с обновлением за последний месяц
- Пользовательский рейтинг ★ и 👍/👎
- Deep-link `#cyberpunk` / `#bg3` + «Поделиться»
- Фоновый muted-трейлер YouTube в hero (десктоп, без reduced-motion)
- Клавиши: `/` поиск, `←` `→` игры, `?` справка, `Esc` закрыть
- Модалки с автофокусом и возвратом фокуса; FAQ, предложение игры (тип «Нейро-озвучка»)
- PWA — можно установить как приложение
- Асинхронная загрузка CSS (preload) для более быстрого FCP

## Запуск

```bash
# из корня репозитория
python3 -m http.server 8099
# открыть http://localhost:8099
```

Любой статический сервер подходит (Live Server, `npx serve`, GitHub Pages).

## Данные

Все ссылки в `js/data.js` ведут на публичные страницы Playground, Nexus Mods, GamesVoice и Telegram.  
GameVoice **не хостит** файлы — только навигация.

Поля перевода: `type`, `status`, `author`, `rating`, `version`, `updated` (YYYY-MM), `name`, `body`, `links`, `install`.

## Разработка

Правки логики — в `js/app.js`, стилей — в `css/style.css` и `css/style-b.css`, данных — в `js/data.js`.  
Сборки нет: правьте исходники и обновляйте cache-bust `?v=` у скрипта в `index.html` при необходимости.

## Планы

- Реальный бэкенд для предложений и оценок
- Страницы авторов
- RSS
- Автопроверка живости ссылок
